# OS
.DS_Store
Thumbs.db

# Local overrides (machine-specific, not tracked)
*.local
.mcp-env

# Backup dirs
.dotfiles_backup/

# iTerm2 binary plist (synced locally, not tracked in git)
iterm2/com.googlecode.iterm2.plist
